// Legacy placeholder: this folder was used in the old Platformatic setup.
// The current backend uses Fastify routes under src/routes/*.js and does not load plugins here.
// We keep a tiny no-op export to satisfy editors and type checkers.

export default function legacySpatialPlugin() {
  // no-op
}
