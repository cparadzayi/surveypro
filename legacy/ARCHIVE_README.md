# Legacy Code Archive

This directory acts as a marker that the previous implementation (original `backend/` and `frontend/`) is now deprecated.

Planned next steps:
- New clean backend in `app-backend/` (Platformatic DB + auth)
- New clean frontend in `app-frontend/` (Vite + Vue 3 + Tailwind + Pinia + Axios)

The existing folders are intentionally retained temporarily for reference until the new MVP stabilizes.

Do not add new code here; place all new work under `app-backend/` and `app-frontend/`.
